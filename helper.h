#include <string>
#include <fstream>
using namespace std;

class VByteWriter {
    private:
        ofstream out;
    public:
        VByteWriter(string file_name) {
        }

        ~VByteWriter() {

        }
};

class VByteReader {
    private:
    public:
        VByteReader(string file_name) {
        }
};